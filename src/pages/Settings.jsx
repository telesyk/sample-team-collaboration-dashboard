import { PageTemplate } from '@/components'

export default function Settings() {
  console.debug('is Settings page')
  return (
    <PageTemplate>
      <h1 className="text-xl">Settings</h1>
    </PageTemplate>
  )
}
