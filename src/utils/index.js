import { login, logout, getCurrentUser } from './mockAuthService'
import { getUserCredentials, setUserCredentials } from './mockUser'

export { getUserCredentials, setUserCredentials, login, logout, getCurrentUser }
